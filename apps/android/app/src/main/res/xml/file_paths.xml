<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Used by FileProvider to expose camera-capture JPEGs back to MediaStore / Camera apps. -->
    <cache-path name="capture_images" path="capture/" />
</paths>
