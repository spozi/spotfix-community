<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:name=".SpotFixApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpotFix"
        tools:targetApi="35">

        <!-- Mirrors iOS Info.plist: SPOTFIX_API_BASE_URL / SPOTFIX_TENANT_SLUG / SPOTFIX_GOOGLE_CLIENT_ID -->
        <meta-data android:name="SPOTFIX_API_BASE_URL"
            android:value="${spotfixApiBaseUrl}" />
        <meta-data android:name="SPOTFIX_TENANT_SLUG"
            android:value="${spotfixTenantSlug}" />
        <meta-data android:name="SPOTFIX_GOOGLE_CLIENT_ID"
            android:value="${spotfixGoogleClientId}" />
        <meta-data android:name="SPOTFIX_FIREBASE_APP_ID"
            android:value="${spotfixFirebaseAppId}" />
        <meta-data android:name="SPOTFIX_FIREBASE_API_KEY"
            android:value="${spotfixFirebaseApiKey}" />
        <meta-data android:name="SPOTFIX_FIREBASE_PROJECT_ID"
            android:value="${spotfixFirebaseProjectId}" />
        <meta-data android:name="SPOTFIX_FIREBASE_SENDER_ID"
            android:value="${spotfixFirebaseSenderId}" />

        <service
            android:name=".notifications.SpotFixFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SpotFix">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Exposes camera-capture files in cache/capture/ back to the system Camera app
             so ActivityResultContracts.TakePicture can write the JPEG to our process. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
